# 《基于物理约束与隐式曲面的智能植物生长模拟与交互式编辑系统》技术栈文档

| 文档版本 | V1.0 |
| --- | --- |
| 编写日期 | 2026-08-03 |
| 编写依据 | 已检测的本地开发环境实测结果 |

---

## 一、技术栈总览

```
┌──────────────────────────────────────────────────────────────────┐
│                         应用层 (应用逻辑)                          │
│    植物生成 · 生长模拟 · 物理约束 · 交互编辑 · 渲染控制             │
├──────────────────────────────────────────────────────────────────┤
│   开发语言: C++17            GUI框架: Qt 5.14.2                    │
├──────────────────────────────────────────────────────────────────┤
│   渲染: OpenGL 4.3+          数学: Eigen 3.4.0                    │
│   (着色器 GLSL 430)          (header-only)                       │
├──────────────────────────────────────────────────────────────────┤
│   构建: CMake 3.16+          编译器: MSVC 19.41 (VS2022)          │
├──────────────────────────────────────────────────────────────────┤
│   AI 推理: ONNX Runtime      训练栈: Python + PyTorch (离线)      │
├──────────────────────────────────────────────────────────────────┤
│   运行平台: Windows 10/11    硬件: NVIDIA RTX 4060 (OpenGL 4.6)   │
└──────────────────────────────────────────────────────────────────┘
```

| 层次 | 技术 | 版本 | 状态（已实测） |
| --- | --- | --- | --- |
| 开发语言 | C++ | C++17 | ✅ 可用 |
| GUI 框架 | Qt | 5.14.2 | ✅ 已装（`D:\Qt\5.14.2`） |
| 三维渲染 | OpenGL | 4.3+（实际 4.6） | ✅ 已确认（RTX 4060） |
| 数学计算 | Eigen | 3.4.0 | ✅ 已装（`third_party/eigen`） |
| 构建系统 | CMake | 3.16+（VS 自带） | ✅ 已确认 |
| 编译器 | MSVC | 19.41（VS2022 Community） | ✅ 已确认 |
| AI 推理 | ONNX Runtime | 1.17+（C++ API） | 待接入（third_party） |
| 训练栈 | Python + PyTorch | Python 3.9+ / PyTorch 2.x | 待安装（离线） |

---

## 二、开发语言：C++17

### 2.1 选择理由

- 高性能数值计算与实时渲染的首选语言，直接映射 GPU 图形 API。
- Qt、OpenGL、Eigen 均提供完善的 C++ 原生接口。
- 支持面向对象（植物节点、渲染器、物理引擎）与泛型编程（Eigen 表达式模板）。

### 2.2 项目中使用到的 C++17 特性

| 特性 | 用途 |
| --- | --- |
| `std::variant` / `std::optional` | 生长决策结果、可选拾取结果 |
| `std::shared_ptr` / `std::unique_ptr` | 植物、网格、着色器资源生命周期管理 |
| `std::thread` / `std::async` | 物理约束多线程并行计算 |
| `std::shared_mutex` | 多线程共享数据（Metaball 集合、环境状态）读写保护 |
| Lambda 表达式 | Qt 信号槽连接、渲染回调 |

---

## 三、GUI 框架：Qt 5.14.2

### 3.1 版本选择

项目要求 "Qt 6 / Qt 5.14+"，本地已安装 **Qt 5.14.2**（`D:\Qt\5.14.2`），处于官方支持范围内，直接采用。

### 3.2 使用的 Qt 模块

| 模块 | 用途 |
| --- | --- |
| `QtWidgets` | 主窗口、控制面板、菜单栏、状态栏 |
| `QtOpenGL` / `QOpenGLWidget` | 三维渲染视口容器 |
| `QOpenGLShaderProgram` | OpenGL 着色器程序封装 |
| `QOpenGLBuffer` / `QOpenGLVertexArrayObject` | 顶点数据管理 |
| `QTimer` | 实时渲染刷新循环（约 60 FPS） |
| `QJsonDocument` | 植物预设与场景配置读写 |
| 信号槽机制 | 界面控件与模拟/渲染模块解耦通信 |

### 3.3 Qt 5 与 Qt 6 差异说明

- CMake 模块名：`Qt5::Widgets` / `Qt5::OpenGL`（Qt6 为 `Qt6::OpenGLWidgets` 等）。
- `QOpenGLWidget` 在 Qt5 位于 `QtWidgets`，Qt6 独立为 `QtOpenGLWidgets` 模块。
- 业务代码 API 基本兼容，未来可平滑升级 Qt6。

---

## 四、三维渲染：OpenGL 4.3+

### 4.1 版本要求

- 项目要求 OpenGL 4.x。
- 本机实测上下文 **OpenGL 4.3.0（NVIDIA 560.94，RTX 4060）**，支持 Compute Shader（4.3 起）。

### 4.2 使用的渲染技术

| 技术 | OpenGL 版本要求 | 项目用途 |
| --- | --- | --- |
| Core Profile + VAO/VBO | 3.3 | 网格渲染 |
| GLSL 着色器 | 4.0 | Phong / PBR / 阴影着色 |
| 深度贴图阴影 | 3.0 | 实时阴影 |
| **Compute Shader** | **4.3** | GPU 并行隐式曲面采样（创新点5） |
| Shader Storage Buffer (SSBO) | 4.3 | Metaball 数据上传 GPU |
| 多重采样 MSAA | 3.2 | 抗锯齿 |

### 4.3 OpenGL 调用方式

- 优先使用 Qt 封装（`QOpenGLShaderProgram` 等）。
- Compute Shader 与底层扩展使用 `QOpenGLFunctions_4_3_Core` 或原生 `glcorearb.h`。

---

## 五、数学计算：Eigen 3.4.0

### 5.1 版本与形态

- **Eigen 3.4.0**（header-only 模板库，无需编译）。
- 已解压至项目 `third_party/eigen`，通过 `#include <Eigen/Dense>` 使用。

### 5.2 项目中的数学用途

| 数据类型 | 用途 |
| --- | --- |
| `Vector3f` | 节点位置、生长方向、法线、光照方向、风力向量 |
| `Matrix4f` | 视图矩阵、投影矩阵、模型变换 |
| `Quaternionf` | 交互旋转（避免万向锁） |
| `AlignedBox3f` | 包围盒、空间限制检测 |
| 数值计算 | Metaball 势函数、Marching Cubes 插值、Perlin Noise 向量运算 |

### 5.3 CMake 集成

```cmake
set(EIGEN_INCLUDE_DIR ${CMAKE_SOURCE_DIR}/third_party/eigen)
target_include_directories(PlantSimulationSystem PRIVATE ${EIGEN_INCLUDE_DIR})
```

---

## 六、构建系统：CMake

### 6.1 版本与工具

- 项目要求 CMake 3.16+。
- 使用 **VS2022 自带 CMake**（`...\CommonExtensions\Microsoft\CMake\CMake\bin\cmake.exe`），无需单独安装。

### 6.2 构建方式

```powershell
# 配置（生成 VS2022 x64 工程，指定 Qt5 路径）
cmake -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH=D:/Qt/5.14.2/msvc2017_64 -S . -B build

# 编译
cmake --build build --config Release
```

### 6.3 关键构建配置

| 配置项 | 值 |
| --- | --- |
| C++ 标准 | C++17 |
| 自动 MOC | `CMAKE_AUTOMOC ON` |
| MSVC 编码 | `/utf-8`（消除中文注释警告） |
| 依赖查找 | `find_package(Qt5 Widgets OpenGL)`、`find_package(OpenGL)` |

---

## 七、编译器：MSVC 19.41

- **MSVC 19.41.34120**（Visual Studio 2022 Community 17.11）。
- 路径：`C:\Program Files\Microsoft Visual Studio\2022\Community\VC\Tools\MSVC\14.41.34120\bin\Hostx64\x64\cl.exe`。
- Qt 5.14.2 官方二进制为 `msvc2017_64` 构建，经实测与 VS2022 MSVC 链接运行**完全兼容**。
- 备选方案：`D:\Qt\Tools\mingw730_64`（Qt 自带 MinGW 7.3，与 Qt `mingw73_64` 完全匹配）。

---

## 八、硬件与运行环境（实测）

| 项 | 实测值 |
| --- | --- |
| 操作系统 | Windows 11（内核 10.0.26100） |
| GPU | NVIDIA GeForce RTX 4060 Laptop GPU |
| 集显 | Intel UHD Graphics |
| OpenGL 驱动 | 4.3.0（NVIDIA 560.94） |
| Qt 位置 | `D:\Qt\5.14.2\msvc2017_64` |
| Eigen 位置 | `third_party/eigen` |

---

## 九、Python 训练与 AI 推理栈

### 9.1 AI 系统技术栈

| 层次 | 技术 | 版本 | 用途 |
| --- | --- | --- | --- |
| 训练框架 | Python + PyTorch | Python 3.9+ / PyTorch 2.x | 训练三个 AI 网络 |
| 数值处理 | NumPy | 1.24+ | 数据预处理与特征工程 |
| 数据生成 | C++ 物理模拟器（离线批量） | 现有技术栈 | 生成训练样本 |
| 模型交换 | ONNX | 1.14+ | 跨语言模型格式 |
| C++ 推理 | ONNX Runtime (C++ API) | 1.17+ | 加载 .onnx 实时推理 |
| 模型管理 | 归一化参数 JSON | — | 特征标准化参数随模型分发 |

### 9.2 AI 三模块模型栈

| AI 模块 | 网络结构 | 训练目标 | 输出 |
| --- | --- | --- | --- |
| 智能生长策略网络 | MLP / GNN | 监督或强化学习 | 生长方向/分支/长度/叶片密度 |
| 植物形态预测 | LSTM / Transformer | 时序监督学习 | 未来节点位置/长度/半径 |
| 植物健康/疾病预测 | 多任务 MLP | 分类+回归 | 健康分 0~1 / 风险等级 |

### 9.3 训练-部署管线（Python ↔ C++）

```
C++ 物理模拟器批量生成数据 (.npz/.csv)
        ↓
Python 预处理（归一化 / 划分训练验证集）
        ↓
PyTorch 训练 (train_policy / train_morphology / train_health)
        ↓
torch.onnx.export → policy_net.onnx / morphology_net.onnx / health_net.onnx
        ↓
拷贝至 resources/models/（含归一化参数 JSON）
        ↓
C++ ONNX Runtime 加载 → 实时推理 → GrowthEngine / UI 使用
```

### 9.4 Python 训练环境依赖

```
requirements.txt:
torch>=2.0
numpy>=1.24
onnx>=1.14
onnxruntime>=1.17
scikit-learn          # 数据划分与评估
matplotlib            # 训练曲线可视化
```

### 9.5 C++ 端 AI 集成

- 依赖：ONNX Runtime（`third_party/onnxruntime`，含头文件与 `onnxruntime.lib/.dll`）。
- 类：`AIGrowthPolicyNetwork`、`MorphologyPredictor`、`HealthPredictor`（详见《详细设计文档》第九章）。
- 与 GrowthEngine 集成：决策模式 Rule / AI / Hybrid 可切换。

---

## 十、技术栈与模块映射

| 系统模块 | 主要技术 | 说明 |
| --- | --- | --- |
| 植物生成模块 | C++17、Eigen | L-System 字符串重写与节点树构建 |
| 隐式曲面模块 | C++17、Eigen、Compute Shader | Metaball 势函数求值与体素场 |
| Marching Cubes | C++17、Eigen | 等值面网格提取 |
| 物理约束模块 | C++17、std::thread、Eigen | 向光/向地/风场/资源模拟 |
| 智能生长引擎 | C++17 | 参数驱动决策模型 |
| AI 智能系统 | PyTorch(训练) + ONNX Runtime(C++推理) | 生长策略/形态/健康三模块预测 |
| 交互编辑模块 | Qt 信号槽、Eigen、四元数 | 拾取、变换、节点编辑 |
| 三维渲染模块 | OpenGL 4.3、GLSL、Qt OpenGL | Phong/PBR/阴影/Compute |
| UI 控制面板 | Qt Widgets、QJsonDocument | 参数调节与配置持久化 |

---

## 十一、技术栈风险与备选

| 风险点 | 说明 | 备选方案 |
| --- | --- | --- |
| Qt msvc2017_64 与 VS2022 非官方配对 | 实测可用，长期开发一般无碍 | 切换 Qt `mingw73_64` + 自带 MinGW |
| Compute Shader 需 OpenGL 4.3+ | RTX 4060 实测 4.3 达标 | CPU 多线程回退路径 |
| 未安装 Qt 6 | 项目要求 5.14+ 即可 | 未来可平滑升级 Qt6 |
| Eigen 为 header-only | 无 ABI 顾虑 | 亦可迁移至 vcpkg 管理 |
| ONNX Runtime 需下载预编译库 | 需与 MSVC 版本匹配 | 自行编译 / 使用 vcpkg |
| AI 训练需 Python 环境 | 依赖 torch 体积较大 | 仅训练机安装，C++ 端只依赖推理库 |

---

## 十二、结论

本项目的技术栈选择**全部经本地实测验证通过**：

- **Qt 5.14.2**（GUI）+ **OpenGL 4.3**（渲染）+ **Eigen 3.4.0**（数学）+ **C++17**（语言）+ **CMake/MSVC**（构建）组合可正常编译、链接、运行。
- 已生成可运行的最小程序骨架（`PlantSimulationSystem.exe`），实测输出 OpenGL 4.3.0 与 Eigen 计算正确。
- **AI 系统**采用"Python/PyTorch 训练 + ONNX 导出 + C++/ONNX Runtime 推理"双端架构，保持现有 C++ 技术栈不变，仅新增 Python 训练部分。

技术栈满足毕业设计文档的全部要求，可直接进入核心模块开发阶段。

---

## 十三、Web 端移植方案

> 本章说明：若本项目未来需部署到 Web 端（浏览器在线演示），需要对现有桌面技术栈做哪些修改。核心算法层可最大程度复用，改动集中在渲染与 UI 层。

### 13.1 两条实现路线对比

| 路线 | 方案 | 核心算法复用度 | 总工作量 | 适用场景 |
| --- | --- | --- | --- | --- |
| **路线 A** | C++ 编译为 **WebAssembly（Emscripten）+ Qt for WebAssembly** | 高（~90%） | 中 | 最短时间获得在线演示版 |
| **路线 B** | **Vue 3 + TypeScript + Three.js / WebGL** 全量原生重写 | 低（0%，需重写） | 高 | 追求 Web 原生体验与生态 |

> **前端框架决策**：Web 端前端框架确定为 **Vue 3**（Composition API + `<script setup>`），配套 **Vite** 构建、**Pinia** 状态管理、**Element Plus** 组件库。UI 层全部基于 Vue 3 实现，不再复用 Qt Widgets。

**推荐路线 A + Vue 3 混合方案**：核心算法（L-System、隐式曲面、Marching Cubes、物理引擎）编译为 WASM 复用，前端 UI 采用 **Vue 3** 通过 Emscripten embind 调用 WASM 核心，实现"Vue 前端 + WASM 计算 + WebGL2 渲染"的 Web 架构。

### 13.2 技术栈对照修改表（桌面 → Web）

| 桌面端技术 | Web 端替代 | 修改内容 | 工作量 |
| --- | --- | --- | --- |
| C++17 | C++ → **WASM**（路线 A）/ TypeScript（路线 B） | 工具链替换 / 全部重写 | 低 / 高 |
| Qt 5.14.2 Widgets | **Vue 3 + Element Plus**（前端 UI） | UI 层用 Vue 3 重写 | 高 |
| QOpenGLWidget | **WebGL canvas**（Three.js / 原生 WebGL 封装） | 渲染容器更换 | 中 |
| OpenGL 4.3 | **WebGL 2.0**（OpenGL ES 3.0）/ WebGPU | 渲染 API 降级 | 中 |
| GLSL 430 | **GLSL ES 300** | 着色器语法迁移 | 中 |
| Compute Shader | WebGL2 不支持 → Fragment Shader 模拟 / WebGPU / CPU 回退 | 隐式场计算改造 | 中 |
| Eigen 3.4.0 | **保留原样**（WASM 直接可用） | 无需修改 | 无 |
| CMake + MSVC | **CMake + Emscripten 工具链** | 构建脚本改造 | 中 |
| QTimer 渲染循环 | **requestAnimationFrame** | 简单替换 | 低 |
| QJsonDocument | **原生 JSON**（Vue 中直接使用） | 简单替换 | 低 |
| std::thread / shared_mutex | **pthread + WASM threads / Web Workers + SharedArrayBuffer** | 多线程改造 | 中 |
| 文件读写（Qt） | **IndexedDB** / File System Access API | 持久化改造 | 中 |

### 13.3 各模块修改细节

#### 12.3.1 渲染模块（改动最大）

- **API 降级**：OpenGL 4.3 → WebGL 2.0（基于 OpenGL ES 3.0）。
- **GLSL 迁移**：`#version 430 core` → `#version 300 es`；`attribute`/`varying` → `in`/`out`；统一 `layout(location=...)`。
- **Compute Shader 替代方案**（创新点5 的 GPU 加速在 Web 端需要调整）：
  1. 用 **Fragment Shader 输出到浮点纹理** 模拟体素场计算；
  2. 升级 **WebGPU**（原生支持 compute pass）；
  3. 退化为 **CPU + WASM 多线程** 计算（`-pthread`）。
- **Phong / PBR / 阴影**：WebGL2 完全支持，仅需 GLSL 语法迁移，逻辑不变。

#### 12.3.2 隐式曲面 + Marching Cubes（核心算法，改动小）

- 纯数学计算 + Eigen，编译为 WASM 后可**原样复用**。
- 仅需：去掉 Qt 依赖、将输入输出改为内存缓冲区传递。
- GPU 部分按 12.3.1 改造。

#### 12.3.3 物理约束 + 智能生长（改动小）

- 纯 C++ 计算，WASM 直接复用。
- 多线程需改用 **Emscripten pthread**（依赖 `SharedArrayBuffer`，服务器需返回 `COOP/COEP` 响应头）。

#### 12.3.4 UI 层（采用 Vue 3 重写，改动大）

前端 UI 确定采用 **Vue 3** 框架，实现如下：

- **框架**：Vue 3（Composition API + `<script setup>`）+ TypeScript。
- **构建**：Vite（开发热更新 + 生产打包，天然支持 WASM 资源）。
- **状态管理**：Pinia（管理植物参数、环境参数、运行状态）。
- **组件库**：Element Plus（参数面板、按钮、滑杆、下拉选择等控件）。
- **3D 视口**：封装为 Vue 组件 `PlantViewport.vue`，内部承载 WebGL canvas。
- **与 WASM 交互**：通过 **Emscripten embind** 将 C++ 类（`Plant`、`GrowthEngine`、`ImplicitSurface` 等）暴露给 JS，在 Vue 组件中调用。

**Vue 组件结构（示意）**：

```
src/
├── main.ts                 # Vue 应用入口
├── App.vue                 # 根组件（三栏布局）
├── components/
│   ├── PlantViewport.vue   # 3D 视口（WebGL canvas + 渲染循环）
│   ├── ControlPanel.vue    # 植物参数 / 环境参数面板
│   ├── PlantList.vue       # 植物类型与预设列表
│   └── StatusBar.vue       # 帧率 / 节点数 / 三角形数
├── stores/
│   └── simulation.ts       # Pinia：模拟状态管理
└── wasm/
    ├── plantModule.ts      # WASM 模块加载与封装
    └── embind.d.ts         # C++ 类的 TS 类型声明
```

#### 12.3.5 交互模块

- `QMouseEvent` → **DOM Pointer Events**。
- 拾取射线、四元数旋转、样条弯曲等**纯数学逻辑不变**。
- 需补充触摸屏 / 触控板事件处理。

### 13.4 新增技术依赖

| 新增项 | 用途 |
| --- | --- |
| **Emscripten (emsdk)** | C++ → WASM 编译器工具链 |
| **WebAssembly** | 核心算法运行时 |
| **WebGL 2.0** | 渲染 API（路线 A 下由 Qt-WASM 封装） |
| **WebGPU**（可选） | 现代 GPU 计算（compute shader 支持） |
| **Vue 3**（前端框架） | UI 层全部基于 Vue 3 实现 |
| **Vite** | 前端构建工具（HMR + WASM 资源支持） |
| **Pinia** | Vue 状态管理（模拟参数/环境状态） |
| **Element Plus** | Vue 组件库（参数面板、控件） |
| **TypeScript** | 前端类型安全与 embind 类型声明 |
| **Three.js**（可选） | WebGL 渲染封装与场景管理 |
| **Web Workers / SharedArrayBuffer** | 多线程并行 |
| **IndexedDB** | 数据持久化 |
| **ONNX Runtime Web**（可选） | 浏览器端 WASM 推理 AI 模型 |

### 13.5 Web 端部署架构

```
┌──────────────────── 浏览器 ────────────────────┐
│  Vue 3 UI (Vite + Pinia + Element Plus)          │
│  ├─ ControlPanel.vue   参数控制面板              │
│  ├─ PlantViewport.vue  3D 视口(WebGL canvas)     │
│  └─ PlantList/StatusBar 列表与状态栏             │
│  ┌──────────────────────────────────────────┐   │
│  │  JS 胶水层 (embind: 参数传递/事件绑定)    │   │
│  └──────────────┬───────────────────────────┘   │
│                 ▼                               │
│  ┌──────────────────────────────────────────┐   │
│  │  WASM 模块 (核心算法)                     │   │
│  │  L-System / 隐式曲面 / Marching Cubes     │   │
│  │  / 物理约束 / 智能生长                     │   │
│  └──────────────┬───────────────────────────┘   │
│                 ▼                               │
│  ┌──────────────────────────────────────────┐   │
│  │  WebGL2 / WebGPU 渲染上下文               │   │
│  │  (Three.js / 原生 WebGL: Phong/PBR/阴影)  │   │
│  └──────────────────────────────────────────┘   │
│  Worker 线程: 物理计算 / 隐式场计算              │
└───────────────────────────────────────────────┘
```

### 13.6 构建脚本修改（Emscripten 示例）

```bash
# 安装 emsdk
git clone https://github.com/emscripten-core/emsdk.git
cd emsdk
emsdk install latest
emsdk activate latest

# 使用 Emscripten 工具链配置
cmake -DCMAKE_TOOLCHAIN_FILE=$EMSDK/upstream/emscripten/cmake/Modules/Platform/Emscripten.cmake \
      -DCMAKE_PREFIX_PATH=$QtWebAssemblyPath \
      -S . -B build-web
cmake --build build-web --config Release
# 产出 .wasm + .js + .html，直接部署到静态服务器
```

CMakeLists.txt 增加条件分支：

```cmake
if(EMSCRIPTEN)
    set(CMAKE_EXECUTABLE_SUFFIX ".html")
    target_link_options(PlantSimulationSystem PRIVATE
        -s WASM=1
        -s USE_WEBGL2=1
        -s PTHREAD_POOL_SIZE=4
        -s EXPORTED_RUNTIME_METHODS=ccall,cwrap
    )
endif()
```

**Vue 前端构建（Vite）**：

```bash
# 初始化 Vue 3 + TypeScript 项目
npm create vite@latest web-frontend -- --template vue-ts
cd web-frontend
npm install pinia element-plus three

npm run dev        # 开发调试（热更新）
npm run build      # 生产打包（dist/ 目录）
```

> 说明：WASM 模块由 `cmake --build build-web` 产出，拷贝到 Vue 项目的 `public/` 目录；前端通过 `fetch('plant.wasm')` + `Module` 初始化，或使用 `vite-plugin-wasm` 简化加载。

### 13.7 Web 端性能注意事项

- **WASM 性能**接近原生（通常为原生 80%~95%），数值核心无明显损耗。
- **多线程依赖** `SharedArrayBuffer`，服务器必须返回 `COOP` / `COEP` 响应头，否则线程池不可用。
- **包体控制**：WASM 模块 + Qt 运行时较大，建议压缩（gzip/brotli）并做按需加载。
- **渲染循环**：用 `requestAnimationFrame` 驱动，替代 `QTimer`。
- **隐式场计算量大**：Web 端建议降低体素分辨率（如 96³~128³），或采用 WebGPU compute 加速。

### 13.8 修改工作量评估

| 模块 | 路线 A（Qt-WASM） | 路线 B（Vue 3 全量重写） |
| --- | --- | --- |
| 植物生成 / 物理 / 智能生长 | ~0%（WASM 复用） | 全部重写（高） |
| 隐式曲面 / Marching Cubes | ~10%（IO 调整） | 全部重写（高） |
| 渲染层 | 中（shader 降级 + compute 替代） | 中（WebGL2 重实现） |
| UI 层 | 中（Vue 3 重写前端） | 高（Vue 3 全量重写） |
| 构建 / 部署 | 中（CMake-WASM + Vite 前端） | 低（纯前端） |
| **总工作量** | **中** | **高** |

### 13.9 推荐建议

若未来部署 Web 版，**推荐"WASM 核心 + Vue 3 前端"混合方案**：

1. 核心算法（L-System、隐式曲面、Marching Cubes、物理引擎）编译为 WASM，**几乎零修改复用**；
2. 前端 UI 采用 **Vue 3 + Vite + Pinia + Element Plus**，通过 embind 调用 WASM 核心；
3. 渲染层改造（WebGL2 GLSL 迁移 + Compute Shader 替代方案），由 `PlantViewport.vue` 组件承载。

该方案兼顾**核心算法复用**与**现代 Web 前端体验**，预计 1~2 周即可完成可在线演示的 Web 版本，适合作为毕业设计的**加分扩展点**。
