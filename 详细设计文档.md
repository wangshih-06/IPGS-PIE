# 《基于物理约束与隐式曲面的智能植物生长模拟与交互式编辑系统》详细设计文档

## 一、文档说明

### 1.1 文档目的

本文档是《基于物理约束与隐式曲面的智能植物生长模拟与交互式编辑系统》的详细设计说明书，在总体项目文档基础上，对系统的各个模块进行深入细化，明确各模块的数据结构、类接口、算法流程、接口规范、错误处理与测试方案，为编码实现提供直接依据。

### 1.2 文档适用范围

本文档适用于本系统的开发人员、测试人员与答辩评审人员，涵盖设计阶段的核心内容。

### 1.3 术语定义

| 术语 | 定义 |
| --- | --- |
| Metaball | 隐式曲面建模中的影响球基元 |
| 隐式势函数 | 描述空间场值的函数 F(p) |
| 等值面 | 势函数满足 F(p)=T 的曲面 |
| L-System | 基于字符串重写规则的植物结构生成系统 |
| Phototropism | 植物向光性 |
| Gravitropism | 植物向地性 |
| LOD | 多层次细节（Level of Detail） |
| SSBO | Shader Storage Buffer Object |

## 二、系统总体设计

### 2.1 分层架构

系统采用五层架构，各层之间通过接口解耦：

```
┌─────────────────────────────────────────────────────────┐
│  表现层 (UI Layer)                                      │
│  MainWindow / ControlPanel / InteractionController      │
├─────────────────────────────────────────────────────────┤
│  智能层 (AI Layer)                                      │
│  AIGrowthPolicyNetwork / MorphologyPredictor            │
│  / HealthPredictor (ONNX Runtime)                       │
├─────────────────────────────────────────────────────────┤
│  逻辑层 (Logic Layer)                                   │
│  GrowthEngine / PhysicsSimulator / PlantManager         │
├─────────────────────────────────────────────────────────┤
│  引擎层 (Engine Layer)                                  │
│  ImplicitSurface / MarchingCube / Renderer              │
├─────────────────────────────────────────────────────────┤
│  数据层 (Data Layer)                                    │
│  PlantNode / MeshData / EnvState / Metaball             │
└─────────────────────────────────────────────────────────┘
```

### 2.2 模块依赖关系

- 表现层依赖逻辑层与引擎层的公开接口。
- 逻辑层依赖数据层的数据结构，并通过引擎层完成建模。
- 引擎层依赖数据层，不直接感知UI。
- 智能层依赖 ONNX Runtime 与数据层特征结构；GrowthEngine 可按决策模式（Rule/AI/Hybrid）调用智能层。
- Python 训练端与 C++ 主程序通过 `.onnx` 模型文件 + 归一化参数 JSON 解耦。

### 2.3 线程模型

- **主线程**：负责UI事件、交互控制、渲染提交。
- **计算线程池**：负责L-System生成、物理约束计算、隐式曲面采样。
- **渲染线程**：负责OpenGL绘制调用。
- **AI 推理线程**：形态/健康预测等耗时推理在后台线程执行，完成后再经信号回到主线程更新 UI（`Ort::Session::Run` 线程安全）。

线程间通过 `std::future`、`QSignalMapper` 或线程安全的命令队列通信。

## 三、公共数据结构设计

### 3.1 数学基础类型

基于 Eigen 定义常用类型别名（存放于 `include/Common/MathTypes.h`）。

```cpp
#include <Eigen/Dense>

using Vec3   = Eigen::Vector3f;
using Vec4   = Eigen::Vector4f;
using Mat3   = Eigen::Matrix3f;
using Mat4   = Eigen::Matrix4f;
using Quat   = Eigen::Quaternionf;
using AABB   = Eigen::AlignedBox3f;

struct BoundingBox {
    Vec3 min;
    Vec3 max;
};
```

### 3.2 植物节点数据结构

```cpp
enum class NodeType { Trunk, Branch, Leaf, Root, Bud };

struct PlantNode {
    uint32_t        id;          // 全局唯一ID
    uint32_t        parentId;    // 父节点ID，根节点为 0
    NodeType        type;        // 节点类型
    Vec3            position;    // 世界坐标位置
    Vec3            direction;   // 生长方向（单位向量）
    float           length;      // 当前长度
    float           radius;      // 半径（随长度衰减）
    float           age;         // 节点年龄
    float           growthSpeed; // 生长速度
    float           vitality;    // 活力值（0~1）
    float           bendAngle;   // 弯曲角度
    float           twistAngle;  // 扭转角度
    std::vector<uint32_t> children; // 子节点ID列表
    std::vector<Metaball> metaballs; // 关联的隐式球
    bool            visible;     // 是否参与渲染
};
```

### 3.3 Metaball 数据结构

```cpp
struct Metaball {
    enum class Type { Trunk, Branch, Leaf, Root, Flower };
    Vec3    center;     // 球心
    float   radius;     // 影响半径
    float   weight;     // 权重
    Type    type;       // 归属类型
    uint32_t ownerId;   // 所属PlantNode ID
};
```

### 3.4 环境状态结构

```cpp
struct EnvironmentParams {
    Vec3    lightDirection; // 光照方向（单位向量，指向光源）
    float   lightIntensity; // 光照强度 0~1
    float   moisture;       // 水分 0~1
    float   nutrition;      // 营养 0~1
    float   temperature;    // 温度（摄氏度）
    float   windIntensity;  // 风力强度 0~1
    float   spaceRadius;    // 空间限制半径
    float   time;           // 模拟时间
};
```

### 3.5 网格数据结构

```cpp
struct MeshData {
    std::vector<Vec3>  vertices;
    std::vector<Vec3>  normals;
    std::vector<Vec2>  uv;
    std::vector<uint32_t> indices;
    BoundingBox        bounds;
    uint32_t           materialId;
};

struct MeshVertex {
    Vec3 position;
    Vec3 normal;
    Vec2 uv;
    uint32_t packed; // 备用
};
```

### 3.6 生长规则结构

```cpp
struct GrowthRule {
    float   branchProbability;  // 分支概率
    float   branchAngle;        // 分支角度（弧度）
    float   lengthGrowthRate;   // 长度增长率
    float   radiusDecay;        // 半径衰减率
    uint32_t maxDepth;          // 最大递归深度
    uint32_t leafPerBranch;     // 每分支叶片数
};

struct GrowthParams {
    float   heightScale;        // 高度缩放
    float   branchiness;        // 分支度 0~1
    float   leafDensity;        // 叶片密度 0~1
    float   rootDepth;          // 根深度
    uint32_t seed;              // 随机种子
    PlantType plantType;        // 植物类型
};
```

## 四、三维植物生成模块详细设计

### 4.1 类设计：LSystemGenerator

```cpp
class LSystemGenerator {
public:
    explicit LSystemGenerator(uint32_t seed);

    // 根据植物类型与参数生成植物结构
    std::shared_ptr<Plant> generate(PlantType type, const GrowthParams& params);

    // 从字符串重写过程构建节点树
    void buildNodeTree(const std::string& axiom,
                       const std::map<char, std::string>& rules,
                       uint32_t iterations);

private:
    std::string applyRewriting(const std::string& input,
                               const std::map<char, std::string>& rules);
    void turtleInterpretation(const std::string& sequence,
                              PlantNode& root,
                              const GrowthRule& rule);
    std::mt19937 rng_;
};
```

### 4.2 改进L-System设计

#### 4.2.1 基本符号集

| 符号 | 含义 |
| --- | --- |
| F | 向前生长一段 |
| f | 向前移动不产生分支 |
| + | 绕X轴正转角度 |
| - | 绕X轴反转角度 |
| & | 绕Y轴俯仰 |
| ^ | 绕Y轴仰角 |
| [ | 压栈（保存当前状态） |
| ] | 出栈（恢复状态） |
| L | 生成叶片 |
| R | 生成根系节点 |
| B | 生成芽（待生长分支点） |

#### 4.2.2 规则示例（松树）

```
Axiom: F
规则:
F -> F[+F]F[-F]F
L -> L
```

#### 4.2.3 改进点

1. **参数化L-System**：为每个符号附加参数（长度、角度、半径），支持连续变化而非离散符号。
2. **随机化重写**：规则选择带概率权重，增强植物形态多样性。
3. **与环境交互**：重写过程中读取环境参数，动态调整分支概率与生长长度。
4. **隐式曲面联动**：每个F符号产生节点后立即生成Metaball，直接对接建模模块。

#### 4.2.4 生成流程伪代码

```
function generate(type, params):
  ruleSet = loadRuleSet(type)
  seq = axiom
  for i in 1..iterations:
    seq = rewrite(seq, ruleSet, env)
  root = new PlantNode()
  turtleInterpret(seq, root, ruleSet.growthRule)
  assignMetaballs(root)
  return buildPlant(root)
```

### 4.3 各植物类型参数表

| 参数 | 松树 | 柳树 | 樱花树 | 灌木 |
| --- | --- | --- | --- | --- |
| 主干高度 | 高（1.0） | 中（0.7） | 中（0.75） | 低（0.4） |
| 分支角度 | 斜上（30°~45°） | 下垂（-20°~-45°） | 水平（15°~30°） | 分散（40°~60°） |
| 分支度 | 中 | 高 | 高 | 极高 |
| 叶片密度 | 高 | 中 | 极高 | 高 |
| 叶片形态 | 针叶 | 长条下垂 | 花瓣状簇 | 圆形叶簇 |
| 根深度 | 深 | 中 | 中 | 浅 |

### 4.4 叶片生成

- 叶片作为独立的PlantNode（`NodeType::Leaf`）。
- 每片叶生成一个或一组Metaball，形成叶面近似。
- 叶片朝向由节点方向与环境光照共同决定。

### 4.5 根系生成

- 根从主干底部向下生成，遵循向地性（Gravitropism）。
- 根节点使用较粗的Metaball，权重较低以形成地下融合形态。

## 五、隐式曲面建模模块详细设计

### 5.1 类设计：ImplicitSurface

```cpp
class ImplicitSurface {
public:
    ImplicitSurface(float threshold = 0.5f, float resolution = 0.05f);

    void clear();
    void addMetaball(const Metaball& ball);
    void removeMetaball(uint32_t ownerId);
    void updateMetaballs(const std::vector<Metaball>& balls);

    // 在空间点 p 处计算隐式势函数值
    float evaluate(const Vec3& p) const;

    // 构建采样场（可指定是否使用GPU加速）
    void buildField(VolumeGrid& grid);

    // 设置体素分辨率与包围盒
    void setResolution(float res);
    void setBounds(const BoundingBox& box);

    // 空间加速结构
    void rebuildOctree();
    bool queryNeighborhood(const Vec3& p, std::vector<const Metaball*>& out) const;

private:
    std::vector<Metaball> balls_;
    float threshold_;
    float resolution_;
    std::unique_ptr<OctreeNode> octree_;
    mutable std::shared_mutex mutex_;
};
```

### 5.2 数学原理

隐式曲面定义为满足下式的点集：

$$
S = \{ p \in \mathbb{R}^3 \mid F(p) = T \}
$$

其中 $T$ 为阈值。使用球形势函数：

$$
g(r) = 
\begin{cases}
\left(1 - \frac{r^2}{R^2}\right)^2 & r \le R \\
0 & r > R
\end{cases}
$$

势函数为：

$$
F(p) = \sum_{i=1}^{N} w_i \cdot g(\|p - c_i\|) - T
$$

- 当 $F(p) > 0$ 时，点位于曲面内部。
- 当 $F(p) < 0$ 时，点位于曲面外部。
- 当 $F(p) = 0$ 时，点位于曲面上。

### 5.3 算法流程

1. 从植物节点树收集所有Metaball。
2. 计算包含所有Metaball的轴对齐包围盒，向外扩一个半径余量。
3. 构建八叉树加速结构。
4. 按分辨率在包围盒内生成采样格点。
5. 对每个格点求势函数值并存储到VolumeGrid。
6. 交由Marching Cubes提取等值面。

### 5.4 采样场VolumeGrid

```cpp
struct VolumeGrid {
    std::vector<float> field;   // 展平的势函数值
    Vec3    origin;             // 体素原点
    Vec3    dims;               // 格点数量
    float   spacing;            // 格距
    BoundingBox bounds;
};
```

### 5.5 计算加速策略

- **CPU路径**：多线程并行处理体素块，利用八叉树剪枝快速跳过无球区域。
- **GPU路径**：Compute Shader中逐格点计算，见第七节。

## 六、Marching Cubes网格生成模块详细设计

### 6.1 类设计：MarchingCube

```cpp
class MarchingCube {
public:
    explicit MarchingCube(const VolumeGrid& grid);

    // 提取等值面网格
    std::shared_ptr<MeshData> extract();

private:
    // 根据体素8个角点的符号位生成配置索引
    uint8_t buildVoxelIndex(const Voxel& v) const;

    // 在边上线性插值顶点
    Vec3 interpolateVertex(const Voxel& v, uint8_t edgeIdx) const;

    // 计算顶点法线（梯度法）
    Vec3 computeNormal(const Vec3& p) const;

    // 优化：移除退化三角形、合并顶点、平滑法线
    void optimizeMesh(MeshData& mesh);

    const VolumeGrid& grid_;
    const uint8_t*  edgeTable_;
    const int8_t*   triTable_;
};
```

### 6.2 算法步骤

1. **三维空间划分**：将包围盒按 `spacing` 划分为 $N_x \times N_y \times N_z$ 个体素。
2. **体素采样**：读取8个角点势函数值，符号决定内/外。
3. **查找表**：8个角点内外组合共 $2^8 = 256$ 种情况，使用 `edgeTable[256]` 与 `triTable[256][16]`。
4. **三角面生成**：根据配置在相交边上插值顶点，生成0~5个三角形。
5. **Mesh优化**：
   - 顶点去重（哈希表索引共享）。
   - 基于梯度计算顶点法线。
   - 删除面积小于阈值的退化三角形。
   - 法线平滑（顶点邻域平均）。

### 6.3 顶点插值公式

边上的顶点位置由线性插值得到：

$$
p = p_1 + \frac{T - F_1}{F_2 - F_1} (p_2 - p_1)
$$

其中 $p_1, p_2$ 为边上两端格点，$F_1, F_2$ 为对应势函数值。

### 6.4 法线计算

利用势函数的梯度作为法线近似：

$$
\mathbf{n}(p) = \frac{\nabla F(p)}{\|\nabla F(p)\|}
$$

使用中心差分近似梯度：

$$
\nabla F(p) \approx 
\begin{bmatrix}
F(p_x+\epsilon) - F(p_x-\epsilon) \\
F(p_y+\epsilon) - F(p_y-\epsilon) \\
F(p_z+\epsilon) - F(p_z-\epsilon)
\end{bmatrix} / (2\epsilon)
$$

### 6.5 伪代码

```
function extract():
  mesh = new MeshData()
  for z in 0..dims.z-1:
    for y in 0..dims.y-1:
      for x in 0..dims.x-1:
        idx = buildVoxelIndex(voxel(x,y,z))
        if idx == 0 or idx == 255: continue   // 完全内外则跳过
        for tri in triTable[idx]:
          for e in tri:
            v = interpolateVertex(voxel, e)
            n = computeNormal(v)
            mesh.addVertex(v, n)
          mesh.addTriangle()
  optimizeMesh(mesh)
  return mesh
```

## 七、物理约束模块详细设计

### 7.1 类设计：PhysicsSimulator

```cpp
class PhysicsSimulator {
public:
    void setEnvironment(const EnvironmentParams& env);
    void setWindField(std::shared_ptr<WindField> wind);

    // 每帧/每时间步更新植物
    void update(std::shared_ptr<Plant> plant, float dt);

private:
    void applyPhototropism(PlantNode& node, const EnvironmentParams& env);
    void applyGravitropism(PlantNode& node, const EnvironmentParams& env);
    void applyWindForce(PlantNode& node, const WindField& wind, float dt);
    void applyGrowthIncrement(PlantNode& node, const EnvironmentParams& env, float dt);
    void propagateToChildren(PlantNode& node);

    EnvironmentParams env_;
    std::shared_ptr<WindField> wind_;
    std::mutex mutex_;
};
```

### 7.2 向光性（Phototropism）

目标方向融合公式：

$$
\mathbf{d}_{growth} = \text{normalize}\left( \alpha \, \mathbf{d}_{current} + \beta \cdot I_{light} \cdot \mathbf{L} \right)
$$

其中：

- $\mathbf{d}_{current}$：当前生长方向。
- $\mathbf{L}$：指向光源的单位向量。
- $I_{light}$：光照强度。
- $\alpha, \beta$：惯性权重与向光权重，满足 $\alpha + \beta = 1$。

遮挡处理：对节点做简单的视锥/半球遮挡判断，若被上层遮挡则削弱 $\beta$。

### 7.3 向地性（Gravitropism）

$$
\mathbf{d}_{final} = \text{normalize}\left( \gamma \, \mathbf{d}_{growth} + \delta \cdot (-\mathbf{g}) \right)
$$

其中 $\mathbf{g}$ 为重力加速度方向（向下），$\gamma, \delta$ 为权重。柳树等垂枝植物 $\delta$ 取值较大。

### 7.4 风场（WindField）

#### 类设计

```cpp
class WindField {
public:
    WindField(uint32_t seed, float scale, float intensity);

    // 在位置 p、时刻 t 采样风向量
    Vec3 sample(const Vec3& p, float t) const;

    void setIntensity(float v);
    void advanceTime(float dt);

private:
    float perlinNoise(const Vec3& p) const;
    uint32_t seed_;
    float scale_;
    float intensity_;
    float time_;
};
```

#### Perlin Noise风场公式

$$
\mathbf{W}(p, t) = \begin{bmatrix}
\text{perlin}(p \cdot k + \mathbf{v} \cdot t) \\
\text{perlin}((p + \delta) \cdot k + \mathbf{v} \cdot t) \\
\text{perlin}((p + 2\delta) \cdot k + \mathbf{v} \cdot t)
\end{bmatrix} \cdot w_{intensity}
$$

- $k$：噪声缩放系数。
- $\mathbf{v}$：风传播速度向量。
- $\delta$：小的偏移用于产生方向差异。

#### 风力作用

对节点施加位置相关的位移：

$$
\Delta p_{node} = \mathbf{W}(p_{node}, t) \cdot m_{node} \cdot dt
$$

同时用弹簧/阻尼模型平滑姿态，避免抖动：

$$
\mathbf{v}_{node} \leftarrow (1 - c) \mathbf{v}_{node} + c \, \mathbf{W}
$$

### 7.5 资源影响

生长速率与资源关系：

$$
v_{growth} = v_0 \cdot f_{moisture}(H) \cdot f_{nutrition}(N) \cdot f_{temp}(T)
$$

- $f_{moisture}(H)$：水分响应函数，超饱和或干旱时下降。
- $f_{nutrition}(N)$：营养响应函数。
- $f_{temp}(T)$：温度响应函数（高斯形，最适温度处最大）。

资源消耗模型：

$$
H_{t+1} = H_t - c_{use} \cdot v_{growth} \cdot dt + c_{recharge} \cdot dt
$$

### 7.6 整体更新流程

```
function update(plant, dt):
  for node in plant.activeNodes:
    d1 = phototropism(node, env)          // 光照调整
    d2 = gravitropism(node, d1)           // 重力弯曲
    d3 = windForce(node, wind, dt)        // 风场扰动
    node.direction = normalize(d2 + d3)
    node.length += v_growth * dt          // 生长增量
    node.vitality = clamp(computeVitality(node, env), 0, 1)
    if vitality < 阈值: node.visible = false   // 衰老枯萎
    updateMetaballs(node)
```

## 八、智能生长引擎详细设计

### 8.1 类设计：GrowthEngine

```cpp
class GrowthEngine {
public:
    GrowthEngine(std::shared_ptr<WindField> wind);

    void setEnvironment(const EnvironmentParams& env);
    void setGrowthParams(const GrowthParams& params);

    // 推进一个生长步长
    void step(std::shared_ptr<Plant> plant, float dt);

    // 输出决策结果
    struct Decision {
        Vec3    growthDirection;
        uint32_t branchCount;
        float   branchLength;
        float   leafDensity;
    };
    Decision decide(const PlantNode& node) const;

private:
    float branchProbability(const PlantNode& node, const EnvironmentParams& env) const;
    float resourceFactor(const EnvironmentParams& env) const;
    uint32_t computeBranchCount(const PlantNode& node) const;

    EnvironmentParams env_;
    GrowthParams params_;
    std::shared_ptr<WindField> wind_;
};
```

### 8.2 决策模型

#### 分支概率

$$
P_{branch} = P_0 \cdot \left(1 - \frac{depth}{maxDepth}\right) \cdot \frac{H + N}{2} \cdot S_{space}
$$

其中 $S_{space}$ 为空间限制因子（越接近空间边界越小）。

#### 分支数量

$$
N_{branch} = \lfloor branchiness \cdot \text{rand}(0,1) \cdot (1 + f_{space}) \rfloor
$$

#### 枝条长度

$$
L_{branch} = L_0 \cdot f_{resource}(H,N) \cdot f_{light}(I_{light})
$$

#### 叶片密度

$$
\rho_{leaf} = \rho_0 \cdot \frac{H}{0.5 + I_{light}} \cdot S_{space}
$$

### 8.3 生命周期状态机

```
enum class LifeStage { Seed, Seedling, Growing, Mature, Senescent, Dead };

state transition:
  Seed       -> Seedling   : time >= t_seedling
  Seedling   -> Growing    : height >= h_seedling
  Growing    -> Mature     : height >= h_mature
  Mature     -> Senescent  : age >= age_senescent
  Senescent  -> Dead       : vitality <= 0
```

每阶段影响生长参数：

| 阶段 | 高度增长率 | 分支概率 | 叶片密度 | 活力 |
| --- | --- | --- | --- | --- |
| 种子 | 0 | 0 | 0 | 1.0 |
| 幼苗 | 高 | 低 | 低 | 1.0 |
| 成长期 | 高 | 高 | 高 | 1.0 |
| 成熟期 | 中 | 中 | 中 | 0.9 |
| 衰老期 | 低 | 低 | 下降 | 递减 |
| 死亡 | 0 | 0 | 0 | 0 |

## 九、AI 智能系统详细设计

### 9.1 AI 系统总体架构

AI 智能系统包含三个核心模块，覆盖"生长策略—形态演化—健康状态"的完整预测链路：

| 模块 | 预测目标 | 输入 | 输出 |
| --- | --- | --- | --- |
| **智能生长策略网络** | 下一阶段最优生长策略 | 环境参数 + 植物状态 | 生长方向、分支数量、枝条长度、叶片密度 |
| **植物形态预测** | 未来 T 时刻的形态 | 初始状态 + 环境参数序列 | 未来节点位置/拓扑/长度/半径 |
| **植物健康/疾病预测** | 健康评分与疾病风险 | 植物状态 + 环境参数 | 健康分(0~1)、风险等级、异常类型 |

**双端架构**：

- **Python 训练端**：使用 PyTorch 训练三个网络，导出 ONNX 模型。
- **C++ 推理端**：使用 ONNX Runtime 加载模型实时推理，与 GrowthEngine 集成。

```
┌──────────────── Python 训练端 ────────────────┐
│ 数据生成(C++模拟器) → 预处理 → PyTorch训练     │
│ → 验证 → torch.onnx.export → .onnx            │
└──────────────────────┬────────────────────────┘
                       ▼
┌──────────────── C++ 推理端 ───────────────────┐
│ ONNX Runtime 加载 → 特征提取 → 实时推理         │
│ → GrowthEngine/渲染/UI 使用预测结果            │
└───────────────────────────────────────────────┘
```

### 9.2 智能生长策略网络设计

#### 9.2.1 类设计：AIGrowthPolicyNetwork

```cpp
// 生长策略输出结构
struct GrowthPolicy {
    Vec3     growthDirection;   // 生长方向
    uint32_t branchCount;       // 分支数量
    float    branchLength;      // 枝条长度
    float    leafDensity;       // 叶片密度
    float    confidence;        // 预测置信度 0~1
};

class AIGrowthPolicyNetwork {
public:
    bool loadModel(const std::string& onnxPath);
    bool isLoaded() const;

    // 根据环境与植物状态预测生长策略
    GrowthPolicy predict(const EnvironmentParams& env,
                         const PlantStateSummary& state) const;

    // 切换决策模式（规则 / AI）
    void setMode(DecisionMode mode);

private:
    std::vector<float> buildInput(const EnvironmentParams& env,
                                  const PlantStateSummary& state) const;
    GrowthPolicy decodeOutput(const float* out) const;

    std::unique_ptr<Ort::Session> session_;
    std::vector<std::string> inputNames_, outputNames_;
    DecisionMode mode_ = DecisionMode::Rule;
};
```

#### 9.2.2 输入特征向量

| 特征 | 维度 | 说明 |
| --- | --- | --- |
| 光照强度 / 方向 | 4 | 光照强度 + 光源方向(x,y,z) |
| 水分 / 营养 / 温度 | 3 | 环境资源 |
| 空间余量 | 3 | 距空间边界的三个方向余量 |
| 当前高度 / 节点数 | 2 | 植物规模 |
| 平均活力 / 生命阶段 | 2 | 植物健康与阶段 |
| 叶面积指数 | 1 | 冠层密度 |

输入维度合计 15。

#### 9.2.3 网络结构（Python/PyTorch）

- **基线 MLP**：3 层全连接（15 → 256 → 256 → 256 → 8），ReLU 激活。
- **增强 GNN**：将植物节点树构建为图，用图卷积聚合邻域状态，输出每节点策略。
- **损失函数**：方向用余弦相似度损失，分支/长度/密度用 MSE。

#### 9.2.4 训练方式

- **监督学习**：以物理模拟器（规则 + 物理约束）生成的最优生长轨迹作为标签。
- **强化学习（可选）**：以"光照捕获量 / 生物量 / 存活度"为奖励，PPO 训练策略网络。

### 9.3 植物形态预测设计

#### 9.3.1 类设计：MorphologyPredictor

```cpp
class MorphologyPredictor {
public:
    bool loadModel(const std::string& onnxPath);
    bool isLoaded() const;

    // 预测未来 T 步形态（T 为环境序列长度）
    PlantMorphology predict(const PlantMorphology& initial,
                            const std::vector<EnvironmentParams>& envSeries) const;

private:
    std::unique_ptr<Ort::Session> session_;
    std::vector<std::string> inputNames_, outputNames_;
};
```

#### 9.3.2 网络结构（Python/PyTorch）

- **时序编码器**：LSTM / GRU（或 Transformer）编码环境参数序列与环境变化趋势。
- **形态解码器**：解码隐状态，逐节点输出未来位置偏移、长度与半径变化。
- **拓扑保持**：仅预测已有节点的位置/尺度演化，新增分支由策略网络决定。

#### 9.3.3 损失函数

$$\mathcal{L} = \lambda_1 \cdot \text{MSE}_{pos} + \lambda_2 \cdot \text{MSE}_{len} + \lambda_3 \cdot \text{MSE}_{rad}$$

其中 $\text{MSE}_{pos}$ 为节点位置均方误差，$\text{MSE}_{len}$、$\text{MSE}_{rad}$ 分别为长度与半径误差。

### 9.4 植物健康/疾病预测设计

#### 9.4.1 类设计：HealthPredictor

```cpp
enum class RiskLevel { Low, Medium, High };
enum class AnomalyType { Drought, NutrientDeficit, HeatStress, ColdStress, SpacePressure };

struct HealthReport {
    float    healthScore;                 // 健康分 0~1
    RiskLevel risk;                        // 疾病风险等级
    std::vector<AnomalyType> anomalies;   // 检测到的异常类型
};

class HealthPredictor {
public:
    bool loadModel(const std::string& onnxPath);
    bool isLoaded() const;

    HealthReport assess(const PlantStateSummary& state,
                        const EnvironmentParams& env) const;

private:
    std::unique_ptr<Ort::Session> session_;
    std::vector<std::string> inputNames_, outputNames_;
};
```

#### 9.4.2 网络结构（Python/PyTorch）

- **多任务网络**：共享主干（MLP），两个输出头——回归头输出健康分（Sigmoid 0~1），分类头输出风险等级（Softmax 3 类）。
- **异常检测**：对输入特征做阈值/注意力分析，辅助输出异常类型。

#### 9.4.3 健康评分模型

$$health = \sigma\left( w_h \cdot \bar{v} + w_r \cdot f(H,N,T) + w_e \cdot (1 - \text{stress}) \right)$$

其中 $\bar{v}$ 为平均活力，$f(H,N,T)$ 为资源适宜度，$stress$ 为环境胁迫强度。

### 9.5 C++ 端推理设计（ONNX Runtime）

#### 9.5.1 依赖与初始化

- 依赖 **onnxruntime** C++ 库（提供静态库/动态库与头文件）。
- 全局 `Ort::Env` 单例，每个模型一个 `Ort::Session`。

#### 9.5.2 推理示例

```cpp
#include <onnxruntime/core/session/onnxruntime_cxx_api.h>

static Ort::Env ortEnv(ORT_LOGGING_LEVEL_WARNING, "plant_ai");

bool AIGrowthPolicyNetwork::loadModel(const std::string& path) {
    Ort::SessionOptions opts;
    opts.SetIntraOpNumThreads(4);
    session_ = std::make_unique<Ort::Session>(ortEnv, path.c_str(), opts);
    // 读取输入/输出名
    return true;
}

GrowthPolicy AIGrowthPolicyNetwork::predict(
        const EnvironmentParams& env, const PlantStateSummary& state) const {
    auto input = buildInput(env, state);
    std::array<int64_t, 2> shape{1, (int64_t)input.size()};
    auto memInfo = Ort::MemoryInfo::CreateCpu(OrtArenaAllocator, OrtMemTypeDefault);
    auto inTensor = Ort::Value::CreateTensor<float>(memInfo, input.data(), input.size(),
                                                    shape.data(), shape.size());
    auto out = session_->Run(Ort::RunOptions{nullptr},
                             inputNames_.data(), &inTensor, 1,
                             outputNames_.data(), 1);
    return decodeOutput(out[0].GetTensorMutableData<float>());
}
```

#### 9.5.3 数据桥接

- Eigen 向量与 `std::vector<float>` 之间的转换工具函数（`AIUtils.h`）。
- 特征归一化参数（均值/方差）在训练端导出并随模型一起加载（JSON 配置）。
- 会话并发安全：`Ort::Session::Run` 可多线程并发调用，无需加锁。

### 9.6 Python 训练管线设计

#### 9.6.1 目录结构

```
python/
├── data_gen/              # 数据生成
│   ├── generate_policy.py # 策略训练数据
│   ├── generate_morphology.py
│   └── generate_health.py  # 含病态样本
├── models/                # 网络定义
│   ├── policy_net.py
│   ├── morphology_net.py
│   └── health_net.py
├── train/                 # 训练脚本
│   ├── train_policy.py
│   ├── train_morphology.py
│   └── train_health.py
├── export/                # ONNX 导出
│   └── export_onnx.py
├── requirements.txt       # torch, numpy, onnx, onnxruntime
└── README.md
```

#### 9.6.2 网络定义示例（policy_net.py）

```python
import torch.nn as nn

class GrowthPolicyNet(nn.Module):
    def __init__(self, input_dim=15, hidden=256, out_dim=8):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(input_dim, hidden), nn.ReLU(),
            nn.Linear(hidden, hidden), nn.ReLU(),
            nn.Linear(hidden, hidden), nn.ReLU(),
            nn.Linear(hidden, out_dim),
        )
    def forward(self, x):
        return self.net(x)
```

#### 9.6.3 训练流程

```
def train(model, loader, epochs, lr=1e-3):
    opt = torch.optim.Adam(model.parameters(), lr)
    loss_fn = CombinedLoss()   # 方向余弦 + 数值 MSE
    for epoch in range(epochs):
        for x, y in loader:
            pred = model(x)
            loss = loss_fn(pred, y)
            opt.zero_grad(); loss.backward(); opt.step()
        # 验证集评估，记录最佳模型
```

### 9.7 数据生成与数据集格式

#### 9.7.1 数据来源

- **主数据源**：C++ 物理模拟器离线批量模式，输出样本到 `data/`。
- **数据增强**：随机扰动环境参数、植物初始参数，扩大分布覆盖。
- **病态样本**：对缺水/缺肥/极端温度进行长时间模拟，标注低健康分与风险等级。

#### 9.7.2 数据集格式（NumPy .npz）

```
data/
├── policy/     # (state, env) -> strategy
│   ├── train.npz   # inputs: Nx15, labels: Nx8
│   └── val.npz
├── morphology/ # (initial, env_series) -> future_morphology
│   ├── train.npz   # initial: NxD, env_series: NxTxE, morph: NxM
│   └── val.npz
└── health/     # (state, env) -> (health_score, risk)
    ├── train.npz   # inputs: Nx17, scores: Nx1, risks: Nx3
    └── val.npz
```

### 9.8 模型部署流程

```
C++ 物理模拟器批量生成训练数据
        ↓  (.npz / .csv)
Python 数据预处理（归一化 / 训练验证划分）
        ↓
PyTorch 训练（policy / morphology / health）
        ↓
验证通过后 torch.onnx.export 导出 .onnx
        ↓
拷贝至 resources/models/（附归一化参数 JSON）
        ↓
C++ ONNX Runtime 加载，集成到 GrowthEngine / 界面
```

#### 9.8.1 ONNX 导出示例

```python
import torch
from models.policy_net import GrowthPolicyNet

model = GrowthPolicyNet(); model.load_state_dict(torch.load("best.pt")); model.eval()
dummy = torch.randn(1, 15)
torch.onnx.export(model, dummy, "policy_net.onnx",
                  input_names=["features"], output_names=["policy"],
                  opset_version=17, dynamic_axes={"features": {0: "batch"}})
```

#### 9.8.2 与 GrowthEngine 集成

- GrowthEngine 新增 `DecisionMode`（Rule / AI / Hybrid）。
- AI 模式下，`decide()` 调用 `AIGrowthPolicyNetwork::predict()` 替代规则决策；Hybrid 模式下按权重融合。
- 形态预测结果用于"生长预览"叠加显示；健康预测结果驱动状态栏与视口着色。

## 十、交互式编辑模块详细设计

### 10.1 类设计：InteractionController

```cpp
enum class TransformMode { None, Translate, Scale, Rotate, Bend, NodeEdit };

class InteractionController : public QObject {
    Q_OBJECT
public:
    explicit InteractionController(QWidget* viewport);

    void setPlant(std::shared_ptr<Plant> plant);
    void setRenderer(std::shared_ptr<Renderer> renderer);

public slots:
    void onMousePress(QMouseEvent* e);
    void onMouseMove(QMouseEvent* e);
    void onMouseRelease(QMouseEvent* e);
    void onWheel(QWheelEvent* e);
    void onKeyPress(QKeyEvent* e);

signals:
    void plantEdited();
    void statusMessage(const QString& msg);

private:
    bool pickNode(const QPoint& pos, uint32_t& outNodeId);
    void beginTransform(TransformMode mode, uint32_t nodeId);
    void updateTransform(const QPoint& pos);

    void applyScale(float factor);
    void applyRotation(const Quat& q);
    void applyBend(const Vec3& target);

    std::shared_ptr<Plant> plant_;
    std::shared_ptr<Renderer> renderer_;
    TransformMode mode_ = TransformMode::None;
    uint32_t activeNode_ = 0;
    QPoint lastPos_;
};
```

### 10.2 交互事件处理机制

#### 9.2.1 拾取（Pick）

- 使用鼠标射线与植物网格求交。
- 射线构造：屏幕坐标 -> 视口矩阵逆变换 -> 世界射线。
- 采用八叉树加速求交，命中最近节点。

#### 9.2.2 事件流程

```
onMousePress(e):
  if e.button == Left:
    nodeId = pick(e.pos)
    if 命中节点:
      mode_ = 根据当前按键状态选择 (Shift=Scale, Ctrl=Rotate, 默认=Bend/Translate)
      activeNode_ = nodeId
      lastPos_ = e.pos
  elif e.button == Middle:
    进入摄像机平移模式
  elif wheel:
    摄像机缩放

onMouseMove(e):
  switch mode_:
    case Scale:  factor = 1 + (dy / viewportHeight) * k; applyScale(factor)
    case Rotate: q = axisAngle(up, dx*k); applyRotation(q)
    case Bend:   target = screenToWorld(e.pos); applyBend(target)
    case NodeEdit: 修改节点角度/长度，重建隐式曲面
  lastPos_ = e.pos
  emit plantEdited()

onMouseRelease(e):
  mode_ = None; 触发网格重建与重绘
```

### 10.3 变换实现

#### 拉伸（Scale Transform）

对以选定节点为根的子树上所有节点位置相对根节点缩放：

$$
p'_i = c + s (p_i - c)
$$

其中 $c$ 为缩放中心（选定节点），$s$ 为缩放因子。

#### 旋转（Quaternion）

构造四元数旋转并应用到子树：

$$
q = \left[ \cos\frac{\theta}{2}, \, \sin\frac{\theta}{2} \, \mathbf{u} \right], \quad p'_i = c + q (p_i - c) q^{-1}
$$

避免万向锁，支持任意轴旋转。

#### 弯曲（空间曲线变换）

- 将节点链参数化为空间曲线（Catmull-Rom样条）。
- 用户拖拽控制点，样条重新拟合。
- 节点沿新曲线重定位：

```
function applyBend(target):
  chain = extractChain(activeNode_)
  curve = fitCatmullRom(chain.positions)
  curve.controlPoints.back() = target
  for i, node in chain:
    node.position = curve.sample(chain.param(i))
```

#### 局部节点编辑

- 选中节点后，通过参数面板调整：角度、长度、半径、叶片密度、分支概率。
- 修改后重新计算子节点位置，并触发隐式曲面增量更新。

## 十一、三维渲染模块详细设计

### 11.1 类设计：Renderer

```cpp
class Renderer : public QOpenGLWidget {
    Q_OBJECT
public:
    void setPlant(std::shared_ptr<Plant> plant);
    void setEnvironment(const EnvironmentParams& env);
    void setCamera(std::shared_ptr<OrbitCamera> camera);

protected:
    void initializeGL() override;
    void resizeGL(int w, int h) override;
    void paintGL() override;

private:
    void setupShaders();
    void uploadMesh(const MeshData& mesh);
    void renderPlant();
    void renderShadowMap();
    void renderSkybox();
    void drawAxes();

    std::shared_ptr<Plant> plant_;
    std::shared_ptr<OrbitCamera> camera_;
    EnvironmentParams env_;
    QOpenGLShaderProgram phongProgram_;
    QOpenGLShaderProgram pbrProgram_;
    QOpenGLShaderProgram shadowProgram_;
    QOpenGLBuffer vbo_, ebo_;
    QOpenGLVertexArrayObject vao_;
    GLuint depthFBO_;
    GLuint shadowTex_;
    Mat4 lightSpaceMatrix_;
};
```

### 11.2 摄像机（OrbitCamera）

```cpp
class OrbitCamera {
public:
    void rotate(float yaw, float pitch);
    void zoom(float factor);
    void pan(const Vec2& delta);

    Mat4 viewMatrix() const;
    Mat4 projMatrix() const;
    Vec3 position() const;

private:
    Vec3 target_;
    float distance_;
    float yaw_ = 0.f;
    float pitch_ = 0.f;
    float fov_ = 60.f;
    float aspect_;
};
```

### 11.3 渲染管线

1. 深度预通道：从光源视角渲染深度到阴影贴图。
2. 主通道：渲染植物几何，采用 Phong 或 PBR 着色。
3. 计算光照：环境光 + 漫反射 + 镜面反射 + 阴影因子。
4. 后处理：可选 SSAO、色调映射。

### 11.4 Phong着色模型

$$
I = I_{ambient} + I_{diffuse} + I_{specular}
$$

$$
I_{diffuse} = k_d \cdot I_L \cdot \max(0, \mathbf{n} \cdot \mathbf{l})
$$

$$
I_{specular} = k_s \cdot I_L \cdot \max(0, \mathbf{r} \cdot \mathbf{v})^{n_{shininess}}
$$

### 11.5 PBR材质

- 基色 `baseColor`（albedo）
- 金属度 `metallic`
- 粗糙度 `roughness`
- 法线贴图 `normalMap`
- 环境光遮蔽 `ao`

使用 Cook-Torrance 微表面 BRDF：

$$
f_r = k_d \frac{c}{\pi} + k_s \frac{D \cdot F \cdot G}{4(\mathbf{n}\cdot\mathbf{l})(\mathbf{n}\cdot\mathbf{v})}
$$

其中 $D$ 为法线分布（GGX），$F$ 为菲涅尔项，$G$ 为几何遮蔽项。

### 11.6 阴影贴图

- 从光源位置生成深度贴图。
- 片段着色器中比较深度，产生阴影因子。
- 使用 PCF 软阴影与偏移量消除阴影痤疮。

### 11.7 绘制调用流程

```
paintGL():
  clear()
  updateLightSpaceMatrix()
  shadowPass()          // 渲染深度
  view = camera.viewMatrix()
  proj = camera.projMatrix()
  bindShader(phongProgram_)
  setUniform(lightPos, view, proj, shadowTex)
  bindVAO()
  drawElements(indices)
  unbind()
```

## 十二、空间加速与性能优化详细设计

### 12.1 八叉树（Octree）

```cpp
struct OctreeNode {
    BoundingBox box;
    bool isLeaf = false;
    std::array<std::unique_ptr<OctreeNode>, 8> children;
    std::vector<uint32_t> metaballIds;
    uint32_t depth = 0;
};

class Octree {
public:
    void build(const std::vector<Metaball>& balls, uint32_t maxDepth = 8);
    void query(const BoundingBox& region, std::vector<uint32_t>& out) const;
    void queryRay(const Vec3& origin, const Vec3& dir, std::vector<uint32_t>& out) const;
};
```

### 12.2 GPU Compute Shader加速

- 将Metaball数组上传到SSBO。
- Compute Shader逐格点并行计算势函数：

```glsl
#version 430 core
layout(local_size_x = 8, local_size_y = 8, local_size_z = 8) in;

layout(std430, binding = 0) buffer MetaballBuffer {
    vec4 data[];   // 每球: xyz=center, w=radius
};
layout(std430, binding = 1) buffer WeightBuffer {
    vec4 wdata[];  // 每球: x=weight
};
layout(std430, binding = 2) buffer FieldBuffer {
    float field[];
};

uniform vec3 gridOrigin;
uniform float spacing;
uniform ivec3 gridDims;
uniform float threshold;

void main() {
    ivec3 gid = ivec3(gl_GlobalInvocationID.xyz);
    if (any(greaterThanEqual(gid, gridDims))) return;
    vec3 p = gridOrigin + vec3(gid) * spacing;
    float sum = 0.0;
    for (int i = 0; i < ballCount; ++i) {
        vec3 c = data[i].xyz;
        float R = data[i].w;
        float r = length(p - c);
        if (r <= R) {
            float t = 1.0 - (r*r)/(R*R);
            sum += wdata[i].x * t * t;
        }
    }
    int idx = (gid.z * gridDims.y + gid.y) * gridDims.x + gid.x;
    field[idx] = sum - threshold;
}
```

### 12.3 多线程计算

- 使用 `std::async` 或线程池并行计算体素分块。
- 植物节点更新与物理约束计算在后台线程执行，完成后以信号通知主线程。

### 12.4 Mesh简化

- 使用顶点聚类（Voxel Grid Clustering）快速简化。
- 可选实现基于二次误差度量（QEM）的边折叠。

### 12.5 LOD

- 根据视距选择不同分辨率的Marching Cubes采样。
- 网格 LOD0/LOD1/LOD2 在植物结构变化时增量重建。

### 12.6 增量更新

- 植物结构变化时仅重建受影响区域（局部Metaball变化 -> 局部体素重采样）。
- 使用脏标记（dirty flag）机制避免全量重建。

## 十三、界面设计

### 13.1 主界面布局

```
┌───────────────────────────────────────────────────────────┐
│  菜单栏: 文件 | 编辑 | 视图 | 植物 | 模拟 | 帮助           │
├──────────────┬──────────────────────────────┬──────────────┤
│  植物列表     │     三维视口 (QOpenGLWidget)  │  属性面板     │
│  - 类型选择   │                              │  植物参数     │
│  - 预设列表   │   (鼠标交互: 旋转/缩放/平移)  │  环境参数     │
│  - 场景树     │                              │  生长控制     │
│              │                              │  材质设置     │
├──────────────┴──────────────────────────────┴──────────────┤
│  状态栏: 帧率 | 节点数 | 三角形数 | 当前工具模式          │
└───────────────────────────────────────────────────────────┘
```

### 13.2 控制面板功能

- 植物参数：类型、种子、高度、分支度、叶片密度、根深度。
- 环境参数：光照强度与方向、水分、营养、温度、风力。
- 生长控制：开始/暂停/重置、时间步长、生命周期显示。
- 材质设置：Phong/PBR切换、颜色、金属度、粗糙度。
- 工具模式：拉伸、弯曲、旋转、节点编辑。

## 十四、类图总结

```
┌───────────────────────────┐
│        Plant              │
├───────────────────────────┤
│ - nodes: vector<PlantNode>│
│ - rootId: uint32_t        │
│ - params: GrowthParams    │
│ - stage: LifeStage        │
│ + generate()              │
│ + updateGrowth(dt)        │
│ + applyTransform()        │
│ + collectMetaballs()      │
└──────────────┬────────────┘
               │ 1..*
               ▼
┌───────────────────────────┐      ┌───────────────────────────┐
│        PlantNode          │      │      ImplicitSurface      │
├───────────────────────────┤      ├───────────────────────────┤
│ + id, parentId, type      │      │ - balls: vector<Metaball> │
│ + position, direction     │◄────►│ - threshold, resolution   │
│ + length, radius, age     │      │ + evaluate(p)             │
│ + children, metaballs     │      │ + buildField(grid)        │
│ + splitBranch()           │      │ + rebuildOctree()         │
│ + updateOrientation()     │      └──────────────┬────────────┘
└───────────────────────────┘                     │ uses
                                                  ▼
┌───────────────────────────┐      ┌───────────────────────────┐
│       GrowthEngine        │      │       MarchingCube        │
├───────────────────────────┤      ├───────────────────────────┤
│ - env, params             │      │ - grid: VolumeGrid        │
│ + step(plant, dt)         │      │ + extract() -> MeshData   │
│ + decide(node)            │      │ - buildVoxelIndex()       │
│ - branchProbability()     │      │ - interpolateVertex()     │
└──────────────┬────────────┘      └──────────────┬────────────┘
               │ uses                             │ uses
               ▼                                  ▼
┌───────────────────────────┐      ┌───────────────────────────┐
│     PhysicsSimulator      │      │          Renderer         │
├───────────────────────────┤      ├───────────────────────────┤
│ - env_, wind_             │      │ - camera, shaders, vao    │
│ + update(plant, dt)       │      │ + initializeGL()          │
│ - applyPhototropism()     │      │ + paintGL()               │
│ - applyGravitropism()     │      │ + renderShadowMap()       │
│ - applyWindForce()        │      └───────────────────────────┘
└──────────────┬────────────┘
               │ uses
               ▼
┌───────────────────────────┐      ┌───────────────────────────┐
│         WindField         │      │   InteractionController   │
├───────────────────────────┤      ├───────────────────────────┤
│ - seed_, scale_, intensity│      │ - mode_, activeNode_      │
│ + sample(p, t)            │      │ + onMousePress()          │
│ + perlinNoise(p)          │      │ + onMouseMove()           │
└───────────────────────────┘      │ + applyScale/rotate/bend  │
                                   └───────────────────────────┘

AI 系统类（第9章）：

┌───────────────────────────┐   ┌───────────────────────────┐
│  AIGrowthPolicyNetwork    │   │     MorphologyPredictor   │
├───────────────────────────┤   ├───────────────────────────┤
│ - session_: Ort::Session  │   │ - session_: Ort::Session  │
│ + loadModel(onnx)         │   │ + loadModel(onnx)         │
│ + predict(env,state)->Policy│  │ + predict(initial,envSeries)│
└────────────┬──────────────┘   └────────────┬──────────────┘
             │ 使用 ONNX Runtime             │
             ▼                               ▼
┌───────────────────────────┐   ┌───────────────────────────┐
│      HealthPredictor      │   │        ONNX Runtime       │
├───────────────────────────┤   ├───────────────────────────┤
│ - session_: Ort::Session  │   │ - Ort::Env, Ort::Session  │
│ + loadModel(onnx)         │   │ + Run() 实时推理           │
│ + assess(state,env)->Report│  └────────────┬──────────────┘
└────────────┬──────────────┘                │
             │ 决策模式(Rule/AI/Hybrid)       │
             ▼                               ▼
┌───────────────────────────────────────────────────────────┐
│                     GrowthEngine                          │
│  decide() 支持 Rule / AI / Hybrid 三种决策模式             │
└───────────────────────────────────────────────────────────┘
```

## 十五、错误处理与异常设计

### 15.1 异常分类

- 参数越界：生长参数、分辨率、网格尺寸非法时抛出 `std::invalid_argument`。
- 资源分配失败：OpenGL缓冲、纹理创建失败时记录日志并回退到默认状态。
- 数值不稳定：Marching Cubes中退化三角形、NaN处理。
- 线程安全：共享数据访问使用 `std::shared_mutex` 保护。

### 15.2 错误恢复策略

- 初始化失败：弹窗提示并显示可用的最小配置。
- 重建失败：保留上一帧有效网格。
- 输入校验：UI层对用户输入参数进行范围钳制。

## 十六、测试设计

### 16.1 单元测试

- `test_implicit_surface.cpp`：势函数正确性、单球/多球融合、阈值边界。
- `test_marching_cube.cpp`：已知简单场（球、平面）的网格数量与顶点位置。
- `test_growth_engine.cpp`：同一种子可复现、分支概率在预期区间、生命周期迁移正确。
- `test_physics.cpp`：向光/向地方向收敛性、风场强度界限。
- `test_ai_policy.cpp`：策略网络输入输出维度、Rule/AI 模式切换、推理延迟。
- `test_ai_morphology.cpp`：形态预测输出维度、空环境序列容错。
- `test_ai_health.cpp`：健康分范围 [0,1]、风险等级映射正确。

### 16.2 集成测试

- 植物生成 -> 隐式曲面 -> 网格提取 -> 渲染全链路无崩溃。
- 交互编辑后网格正确重建。
- AI 策略 -> 生长引擎 -> 隐式曲面 -> 网格 -> 渲染全链路无崩溃。
- 形态预测预览与健康状态着色正确叠加到视口。

### 16.3 性能测试

- 帧率：目标 ≥ 60 FPS（中等复杂度场景）。
- 网格生成时间：128³ 体素 < 100ms（GPU路径）。
- 内存占用：单株植物 < 500MB。

### 16.4 验收标准

| 指标 | 目标 |
| --- | --- |
| 支持植物类型 | ≥ 4 种 |
| 生命周期阶段 | ≥ 5 阶段 |
| 物理约束 | ≥ 3 类（光/重/风） |
| 交互操作 | ≥ 4 种（拉/弯/转/节点编辑） |
| 渲染 | Phong + PBR + 阴影 |
| 实时帧率 | ≥ 30 FPS |
| AI 模块 | 策略网络/形态预测/健康预测 三模块可用 |
| AI 推理延迟 | 策略 < 16ms，健康 < 10ms |
| 形态预测误差 | 节点平均位置误差 < 8% |

## 十七、构建与部署设计

### 17.1 CMake配置要点

```cmake
cmake_minimum_required(VERSION 3.16)
project(PlantSimulationSystem LANGUAGES CXX)

find_package(Qt5 COMPONENTS Widgets OpenGL REQUIRED)
find_package(OpenGL REQUIRED)
find_package(Eigen3 REQUIRED)
# AI 推理：ONNX Runtime（提供 onnxruntime.lib 与头文件目录）
set(ONNXRUNTIME_DIR ${CMAKE_SOURCE_DIR}/third_party/onnxruntime)

add_executable(PlantSimulationSystem
    src/main.cpp
    src/Plant/Plant.cpp
    src/Plant/PlantNode.cpp
    src/Physics/PhysicsSimulator.cpp
    src/Physics/WindField.cpp
    src/Algorithm/ImplicitSurface.cpp
    src/Algorithm/MarchingCube.cpp
    src/Algorithm/LSystemGenerator.cpp
    src/Rendering/Renderer.cpp
    src/Rendering/Camera.cpp
    src/AI/AIGrowthPolicyNetwork.cpp
    src/AI/MorphologyPredictor.cpp
    src/AI/HealthPredictor.cpp
    src/AI/AIUtils.cpp
    src/UI/MainWindow.cpp
    src/UI/InteractionController.cpp
    src/UI/ControlPanel.cpp
)

target_include_directories(PlantSimulationSystem PRIVATE include ${EIGEN3_INCLUDE_DIR} ${ONNXRUNTIME_DIR}/include)
target_link_libraries(PlantSimulationSystem PRIVATE Qt5::Widgets Qt5::OpenGL OpenGL::GL ${ONNXRUNTIME_DIR}/lib/onnxruntime.lib)
target_compile_features(PlantSimulationSystem PRIVATE cxx_std_17)
```

### 17.2 平台与环境

- 开发环境：Windows 10/11 + MSVC 或 MinGW。
- 运行时要求：OpenGL 4.3+（Compute Shader需要）。
- AI 推理依赖：ONNX Runtime 运行时库（onnxruntime.dll）。
- Python 训练环境（离线）：Python 3.9+、PyTorch、NumPy、ONNX。
- 部署形式：打包可执行文件 + 资源目录（含 resources/models/*.onnx）。

## 十八、附：核心接口清单

| 接口 | 归属 | 说明 |
| --- | --- | --- |
| `ImplicitSurface::evaluate(p)` | 引擎层 | 势函数求值 |
| `ImplicitSurface::buildField(grid)` | 引擎层 | 体素场构建 |
| `MarchingCube::extract()` | 引擎层 | 网格提取 |
| `LSystemGenerator::generate(type, params)` | 逻辑层 | 植物生成 |
| `GrowthEngine::step(plant, dt)` | 逻辑层 | 生长推进 |
| `PhysicsSimulator::update(plant, dt)` | 逻辑层 | 物理更新 |
| `WindField::sample(p, t)` | 逻辑层 | 风场采样 |
| `Renderer::paintGL()` | 表现层 | 渲染帧 |
| `InteractionController::onMouseMove(e)` | 表现层 | 交互处理 |
| `Plant::collectMetaballs()` | 数据层 | 汇总隐式球 |
| `AIGrowthPolicyNetwork::predict(env,state)` | 智能层 | 智能生长策略预测 |
| `MorphologyPredictor::predict(initial,envSeries)` | 智能层 | 未来形态预测 |
| `HealthPredictor::assess(state,env)` | 智能层 | 健康/疾病评估 |

## 十九、后续开发说明

本文档各模块可独立开发与测试，建议实现顺序：

1. 数学基础与数据结构（第3章）。
2. 隐式曲面 + Marching Cubes（第5、6章），先实现可渲染的静态球体。
3. L-System植物生成（第4章）。
4. 物理约束与智能生长（第7、8章）。
5. 渲染与材质完善（第11章）。
6. 交互编辑（第10章）。
7. Python 数据生成与 AI 训练（第9章 + python/ 目录），导出 ONNX 模型。
8. C++ 端 ONNX Runtime 推理集成（第9章），接入 GrowthEngine 与界面。
9. 性能优化（第12章）。
10. 测试与打磨（第16章）。
